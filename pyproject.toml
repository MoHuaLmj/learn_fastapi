[project]
name = "pps_s_first_fastapi_app"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.fastapi]
entrypoint = "first_step.main:app"